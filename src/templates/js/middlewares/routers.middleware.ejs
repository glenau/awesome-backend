import routers from '../routers/index.js';
import { bodyValidator } from '../validators/validator.js';

function setupRouters(app) {
    app.use('/api', bodyValidator, routers);
}

export default setupRouters;