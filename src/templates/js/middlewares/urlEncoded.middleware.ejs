/*
    Middleware for parsing URL encoded body
*/

<% if(webFramework === 'express') { -%>
import { urlencoded } from 'express';

function setupUrlEncodedParser(app) {
    app.use(urlencoded({ extended: true }));
}

export default setupUrlEncodedParser;
<% } -%>
