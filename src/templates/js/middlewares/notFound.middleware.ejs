import Response from '../utils/response.js';

function setupNotFound(app) {
    app.use((req, res, next) => {
        Response.failure(req, res, 'Page not found', 404);
    });
}

export default setupNotFound;
